# XOAUTH2 Proxy v2.0 - Production Dependencies
# Python 3.8+ required

# Core async SMTP server (legacy, replaced by asyncio.Protocol)

# Async SMTP client for upstream relay (NEW - for connection pooling and 50k+ msg/min)
aiosmtplib>=3.0.0

# Async HTTP client for OAuth2 token refresh (PHASE 1: Replaced blocking requests library)
aiohttp>=3.8.0

# Network interface enumeration for source IP binding (optional but recommended)
netifaces>=0.11.0

# Optional: HTTPS/TLS support (included in stdlib but listed for clarity)
# ssl (built-in)

# For development and testing (optional)
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# pytest-cov>=4.0.0
